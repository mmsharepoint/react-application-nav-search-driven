export interface IPermissionItem {
    // thumbnail: string;
    key: string;
    name: string;
    description: string;
    // color: string;
    // shape: string;
    url: string;
    permission: string;
    width?: number;
    height?: number;
}